[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/selfhosted.Dockerfile"

[deploy]
startCommand = "chmod +x /app/bin/* && /app/bin/docker_start"
healthcheckPath = "/healthcheck"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
